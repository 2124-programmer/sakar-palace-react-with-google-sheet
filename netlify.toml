[build]
  command = "npm run build"
  publish = "dist"
  ignore = "if [ \"$BRANCH\" != \"main\" ]; then echo 'Skipping deploy for non-main branch'; exit 0; else exit 1; fi"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
